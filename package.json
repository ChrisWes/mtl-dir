{
  "name": "tech-leaders-directory",
  "private": true,
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "~5.7.2",
    "wrangler": "^3.99.0"
  },
  "scripts": {
    "dev": "wrangler pages dev frontend/dist --d1=DB",
    "build": "cd frontend && npm run build",
    "deploy": "npm run build && wrangler pages deploy frontend/dist"
  }
}
